<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <h6>Slug: {{postagem.slug}}</h6>
        <small>Data de publicação: {{postagem.date}}</small>
        <hr>
        <h6>Descrição: {{postagem.descricao}}</h6>
        <hr>
        <small>{{postagem.conteudo}}</small>
    </div>
</div>